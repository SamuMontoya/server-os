// Dashboard "AGENTIC OS" (vista ORQUESTADOR).
//
// Vivía en "/" hasta 2026-08-31. Se movió aquí porque la raíz pasó a abrir el
// Laboratorio, que es donde Samu trabaja de verdad: al abrir la app el chat
// tiene que estar YA delante, no a un clic. El dashboard no se tocó, solo
// cambió de dirección — la monta el AppShell (app/providers.tsx) igual que
// antes, y esta página solo marca la ruta.
export default function OsHome() {
  return null;
}
